{
  "name": "bgs-invoice-generator",
  "version": "1.0.0",
  "description": "BGS GST Invoice Generator",
  "scripts": {
    "build": "cd frontend && npm install --include=dev && ./node_modules/.bin/vite build && cp -r dist/* ../backend/public/",
    "start": "cd backend && node server.js",
    "postinstall": "cd backend && npm install"
  }
}
